# Moteur de rappel sémantique (OPTIONNEL).
# Sans ces paquets, C Brain retombe automatiquement sur BM25 (lexical), qui
# marche très bien. Ne les installe que si tu veux un rappel par embeddings.
# Volontairement léger : model2vec = embeddings statiques, SANS PyTorch.
model2vec
numpy
